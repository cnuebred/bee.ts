import { Bee as bee, Component as component } from './bee'
import { base_style } from './scripts'

export const Bee = bee
export const Component = component

export const scheme = {
    base_style: base_style,
}
